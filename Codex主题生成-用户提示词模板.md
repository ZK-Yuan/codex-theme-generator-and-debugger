# Codex 主题生成提示词模板

> 使用方法：只修改最前面的“用户填写区”，然后添加图片、PDF、Logo、截图等附件，最后将整份内容发送给 Codex。  
> “固定执行指令”通常不需要修改。

---

# 一、用户填写区

## 1. 主题基本信息

**主题名称：**

> 【请填写，例如：极简黑金、赛博故宫、个人实验室、学校主题】

**目标平台：**

> 【请填写：macOS、Windows，或者“以当前电脑为准”】

**输出位置：**

> 【可选。填写本地目录；留空时由 Codex 在当前工作区创建独立主题项目】

**交付范围：**

> 【请选择：自动判断 / 只制作主题包 / 制作完整可运行的主题工具】

## 2. 自然语言描述

**我想要的整体感觉：**

> 【请直接用自然语言描述，不需要专业设计术语】
>
> 示例：  
> 我想要一套深色、克制、有工程科技感的 Codex 主题。整体以深蓝黑色为底，辅以低饱和青蓝色，不要过度霓虹。首页可以有较明显的视觉元素，但任务页面必须安静、清晰，适合长时间阅读代码。Logo 放在右侧视觉区域，不要做成悬浮卡片，也不能遮挡输入框。

**必须保留的元素：**

> 【请填写，没有则写“无”】
>
> 可以包括：
>
> - 品牌颜色
> - Logo、校徽或标准字
> - 标语或校训
> - 指定字体
> - 建筑、人物、纹样或几何元素
> - 指定明暗模式
> - 必须保留的 Codex 原生区域

**不希望出现的效果：**

> 【请填写，没有则写“无”】
>
> 例如：
>
> - 不要大面积霓虹光
> - 不要随机网格
> - 不要把 Logo 放进矩形卡片
> - 不要让装饰穿过代码和正文
> - 不要改变 Codex 原生控件的位置
> - 不要生成或伪造未经授权的标志

**首页与任务页的偏好：**

> 【可选。留空时默认采用“首页视觉较丰富，任务页安静且强调阅读”的方案】

**动效偏好：**

> 【请选择：关闭 / 轻微 / 明显但不影响操作】

## 3. 素材与授权

**参考素材列表：**

> 【上传附件后填写附件名称，或者填写本地绝对路径；可以继续增加】
>
> 1. 【附件或文件路径】
> 2. 【附件或文件路径】
> 3. 【附件或文件路径】

素材可以包括：

- 视觉识别手册 PDF
- Logo、校徽、标准字
- PNG、JPEG、WebP、SVG
- 页面截图和 UI 参考图片
- 配色参考图
- 字体文件
- Markdown、Word 或其他需求文档
- 已有 Codex 主题项目

# 二、固定执行指令

请使用 `$codex-theme-generator`，根据上述自然语言需求和全部参考素材，从零构建一套专属于我的 Codex 桌面主题。

如果实机应用后出现颜色断层、文字异常底色、Logo 遮挡、输入框周围色块、弹窗消失、侧栏错位、任务页可读性下降、刷新后主题失效或无法完整恢复等问题，请自动使用 `$codex-theme-debugger` 定位、修复并完成回归验证。

## 1. 判断构建范围

先检查当前工作区：

1. 如果已经存在兼容的 Codex 主题引擎，只创建新的主题包、运行时素材、静态预览、测试，以及该设计确实需要的最小运行时适配。
2. 如果不存在主题引擎，并且用户要求完整可运行的主题工具，则创建：
   - 声明式主题包；
   - 静态主题预览；
   - 官方 Codex 应用检测；
   - 安全的本地 CDP 启动和目标验证；
   - 主题应用、热切换、暂停、恢复；
   - 页面刷新和路由变化后的自动恢复；
   - 自动验证、隐私处理和截图功能。
3. 如果用户只允许静态预览，则完成视觉系统、主题包和完整预览矩阵，同时明确哪些实机项目尚未验证。

静态 HTML 预览只能作为设计检查点，不能作为真实 Codex 兼容性的证明。

## 2. 盘点并理解全部输入

读取用户的完整描述和所有附件，不要只查看文件名或缩略图。

将每份素材分类为：

- 用户需求或自然语言描述；
- 官方视觉规范；
- 可以直接使用的品牌素材；
- 设计风格参考；
- Codex 页面或问题截图；
- 字体或矢量源文件；
- 现有代码或主题实现。

不要把截图中的 Logo 自动视为可直接使用的原始素材，也不要把设计参考图片描述成官方品牌规范。

对素材执行：

1. 生成文件清单、类型、大小和哈希。
2. 识别重复文件、缺失文件和无法读取的文件。
3. 对 PDF 同时进行版式文本提取和关键页面渲染检查。
4. 对图片检查原始尺寸、纵横比、透明通道、内嵌背景、裁切、安全留白和颜色模式。
5. 对 Logo、校徽和标准字检查是否存在官方透明版、反白版、横式或竖式组合。
6. 对现有代码检查主题 Schema、素材目录、选择器契约、CDP 边界、测试和未提交修改。

OCR 只能作为没有文字层时的补充，不能代替对 PDF 页面和图像的视觉检查。

## 3. 建立事实、来源和授权体系

把所有视觉结论分为：

- **官方值**：由权威视觉手册或官方素材明确规定；
- **推导值**：根据官方数据计算得到，例如 RGB 转 HEX；
- **产品值**：为了 Codex 可读性和交互体验新增；
- **临时假设**：材料不足时暂时采用，等待确认。

每个重要决定都要记录：

- 来源文件；
- PDF 页码或图片名称；
- 提取值；
- 来源类别；
- 可信程度；
- 允许使用和再分发的范围。

如果材料之间存在冲突：

1. 不要静默选择其中一个。
2. 记录冲突双方及解决方式。
3. 涉及品牌身份、受保护标志或公开授权时，暂停并请求用户确认。
4. 仅涉及可恢复的界面参数时，可以标记假设后继续。

## 4. 生成设计基础文档

在输出项目中生成：

```text
theme-brief.md
references/
├── source-manifest.json
├── visual-facts.md
├── visual-tokens.md
└── asset-ledger.md
```

其中：

- `theme-brief.md`：记录主题定位、氛围、页面差异、品牌强度、禁用方向、动效和完成标准。
- `source-manifest.json`：记录全部输入文件及哈希。
- `visual-facts.md`：记录官方事实、冲突、页码和证据。
- `visual-tokens.md`：记录颜色、文字、面板、透明度、模糊、圆角、间距、动效和响应式规则。
- `asset-ledger.md`：记录素材来源、转换过程、授权和再分发状态。

视觉事实没有整理清楚前，不要直接开始大范围注入 CSS。

## 5. 将自然语言转化为视觉系统

把用户的形容词转化为可以实现和验证的设计参数，包括：

- 主色、辅助色和亮度范围；
- 背景与面板材质；
- 线条、圆角和几何语言；
- 页面密度与留白；
- 品牌元素的出现频率；
- 首页和任务页的视觉强度；
- 动效速度、范围和降级策略；
- Logo、标题、输入框和正文的安全区域。

不要默认使用“黑底、霓虹蓝、随机网格、强光晕”的通用 AI 风格。设计结果应当来源于用户描述和参考素材。

必须区分：

```text
official.*  官方品牌令牌
derived.*   推导令牌
product.*   Codex 界面令牌
semantic.*  原生成功、警告、错误和焦点语义
```

不得使用品牌色覆盖原生警告、错误、审批、权限和焦点语义。

## 6. 正确处理品牌和图片素材

1. 原始素材必须保持不变。
2. 运行时使用的优化素材保存为单独副本。
3. 优先使用官方完整组合标志，不要自行重排 Logo、校名和英文名。
4. 未经允许不得重绘、变形、拉伸、裁切、重新打字、改色或让受保护标志参与变形动画。
5. 只有在能够准确保留前景时，才能移除图片中的矩形背景。
6. 装饰纹理和受保护标志必须分层制作。
7. 不得在运行时主题中引用远程 URL、绝对路径、目录穿越、越界符号链接或可执行素材。
8. 每项素材转换都必须写入素材台账。

## 7. 设计完整页面状态矩阵

至少设计并验证：

| 页面状态 | 设计要求 |
|---|---|
| 首页，侧栏打开 | 完整主题身份、一次品牌标志、明确输入框安全区 |
| 首页，侧栏收起 | 品牌和构图正确重排，不与原生控件冲突 |
| 任务页 | 隐藏或大幅弱化品牌，提供稳定阅读底板 |
| 普通输入框 | 周围颜色连续，无矩形阴影或渐变断层 |
| 附件展开输入框 | 装饰不与可变高度输入框重叠 |
| 窄窗口 | 优先隐藏次要装饰，不压缩受保护标志 |
| 菜单和弹窗 | 保留原生定位、层级、交互和关闭行为 |
| 减少动态效果 | 关闭不必要的循环动画和位移 |

相关视觉元素应共享同一坐标系统。Logo 与圆环、框线或中心点的关系必须通过真实边界框验证，不能只凭目测。

## 8. 构建主题包和静态预览

主题包必须是声明式的，并至少包含：

- Schema 版本；
- 主题 ID、名称和描述；
- 明暗模式；
- 本地背景和品牌素材路径；
- 构图焦点和安全区域；
- 首页和任务页模式；
- 品牌、产品和文字颜色；
- 模糊、透明度、圆角和动效；
- 素材来源与再分发说明。

静态预览必须使用与运行时相同的主题令牌和素材，并提供：

- 首页/任务页切换；
- 侧栏打开/收起；
- 普通/展开输入框；
- 窄窗口；
- 减少动态效果；
- 代表性菜单或弹窗。

## 9. 安全实现真实 Codex 主题

如果需要完整运行时：

1. 验证官方 Codex 应用的 Bundle ID、签名身份、可执行文件和实际进程。
2. CDP 只能监听 `127.0.0.1` 的临时端口。
3. 验证监听端口属于已核验的官方应用进程。
4. 只接受经过验证的主 `type=page`、`app://` 页面和回环 WebSocket。
5. 主题包只能包含声明式 JSON 和本地静态素材。
6. 使用命名空间明确的运行时 class，不使用全局宽泛 CSS。
7. 所有装饰层设置 `pointer-events: none`。
8. 支持主题应用、热切换、暂停、恢复和完整清理。
9. 页面刷新和路由变化后的重新应用必须幂等。

绝对禁止修改：

- 官方 `.app`；
- `app.asar`；
- 官方代码签名；
- 模型或 API 设置；
- 登录状态；
- Codex 网络行为。

## 10. 保护原生布局和交互

维护独立的选择器契约模块，为每个目标记录：

- 语义名称；
- 选择器；
- 页面范围；
- 稳定程度；
- 是否必须存在；
- 注入的主题 class。

对容易误识别的元素增加位置、尺寸和祖先关系检查。

禁止使用会污染全局的规则，例如：

- `span`
- `mark`
- `[data-state]`
- `[class*=selected]`
- 未加几何判断的全部 `nav[aria-label]`

不要修改原生弹窗、菜单和 Floating UI 定位包装器的：

- `position`
- `inset`
- `top`、`right`、`bottom`、`left`
- 原生 `transform`
- 原生 `z-index`
- 测量用宽高
- Portal 所需 overflow

## 11. 实机验证与自动修复

按照从低到高的可信度验证：

1. Schema、素材路径、文件大小和授权字段。
2. 静态预览全部页面状态。
3. 项目构建和自动测试。
4. 安全应用到真实 Codex。
5. 检查计算样式和真实边界框。
6. 截取新的首页与任务页截图。
7. 使用真实点击打开菜单或弹窗，并保持至少三秒。
8. 验证侧栏、快速跳转目录、输入框展开、窄窗口和减少动态效果。
9. 验证路由切换和页面刷新。
10. 验证暂停、恢复和完全恢复原生外观。

如果出现以下问题，自动使用 `$codex-theme-debugger`：

- 顶部黑色渐变带；
- 侧栏与工作区颜色接缝；
- 快速跳转白条附近出现独立色块；
- 输入框周围出现大矩形色块；
- 普通文字出现异常背景或下划线；
- Logo 被裁切、遮挡或错位；
- 菜单、剩余用量窗口或弹窗闪现后消失；
- 任务页装饰穿过正文、代码或终端；
- 刷新后皮肤消失；
- 主题无法完整清理。

每次修复后必须重新读取计算样式、重新截图并完成相邻页面回归，不能只确认源代码中出现了新的 CSS。

## 12. 完成标准

最终主题必须满足：

- 首页能够体现主题身份，但品牌标志只出现一次。
- 任务页明显弱化装饰，并优先保证长文本、代码、Diff 和终端可读性。
- 输入框扩大或添加附件后不与 Logo 和装饰重叠。
- 侧栏收起后构图能够正确重新计算。
- Logo 不变形、不裁切、不低于规范最小尺寸。
- 不存在顶部、侧栏、快速跳转目录、输入框和底部粘性区域的颜色断层。
- 普通文字不会被宽泛选择器错误填充底色。
- 菜单和弹窗不会闪现后消失。
- 支持 `prefers-reduced-motion`。
- 不产生横向页面溢出。
- 路由切换和刷新后主题仍然正确。
- 能够完整恢复官方原生外观。
- 官方 Codex 应用包保持未修改状态。

## 13. 最终交付

完成后提供：

1. 可以直接运行的完整项目或主题包。
2. 静态主题预览。
3. 主题设计简报、视觉事实和视觉令牌。
4. 素材来源、处理记录和授权台账。
5. 自动测试与实机验证结果。
6. 首页、任务页和关键交互状态截图。
7. 清晰、可复制的操作命令：
   - 安装；
   - 构建；
   - 预览；
   - 启动并应用主题；
   - 热切换；
   - 暂停；
   - 恢复；
   - 完全恢复原生外观。
8. 尚未确认的假设和公开再分发限制。

不要在只完成静态预览时宣布任务完成。只有完成用户允许范围内的构建、实机验证、问题修复和恢复测试后，才能交付最终结果。
